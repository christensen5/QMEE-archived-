#include <stdio.h>

int main (void)
{

	int a = 5;
	int b = 2;
	int c = 0;
	int d = 0;

	//  ! NOT (unary: one operand)
	
	if (!a) {
		printf("a is zero\n");
	}
	else if (!d) {
		printf("d is zero\n");
	}

	return 0;
}
